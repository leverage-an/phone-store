# 🚀 วิธีเชื่อมต่อโดเมนกับ Vercel

คุณได้ซื้อโดเมน **ran-torasap.com** แล้ว ตอนนี้ต้องเชื่อมต่อกับเว็บไซต์

## ขั้นตอนการ Setup

### 1. ✅ ยืนยันอีเมล (Email Verification)
- ตรวจสอบอีเมลที่ลงทะเบียนไว้
- เปิดอีเมลจาก Vercel/ICANN สำหรับยืนยันโดเมน
- คลิกลิงก์ยืนยันในอีเมล

### 2. 📤 Deploy Project ขึ้น Vercel (ถ้ายังไม่ได้ deploy)

#### วิธีที่ 1: Deploy ผ่าน GitHub (แนะนำ)

**ก. สร้าง GitHub Repository:**
```bash
# ในโปรเจคของคุณ
git init
git add .
git commit -m "Ready for deployment"
```

**ข. สร้าง Repository บน GitHub:**
1. ไปที่ [github.com](https://github.com)
2. คลิก "New repository"
3. ตั้งชื่อ repository (เช่น `phone-store`)
4. สร้าง repository (ไม่ต้องเพิ่ม README)

**ค. Push code ขึ้น GitHub:**
```bash
git remote add origin https://github.com/YOUR_USERNAME/phone-store.git
git branch -M main
git push -u origin main
```

**ง. Deploy บน Vercel:**
1. ไปที่ [vercel.com/dashboard](https://vercel.com/dashboard)
2. คลิก "Add New Project"
3. เลือก repository ที่เพิ่งสร้าง
4. Vercel จะ detect Next.js อัตโนมัติ
5. **ตั้งค่า Environment Variables** (ถ้าต้องการ):
   - `SMTP_HOST` = smtp.gmail.com
   - `SMTP_PORT` = 587
   - `SMTP_USER` = your-email@gmail.com
   - `SMTP_PASS` = your-app-password
6. คลิก "Deploy"

**ผลลัพธ์:** จะได้ URL เช่น `https://phone-store.vercel.app`

---

#### วิธีที่ 2: Deploy ผ่าน Vercel CLI

```bash
# ติดตั้ง Vercel CLI
npm i -g vercel

# Login
vercel login

# Deploy (เลือก defaults)
vercel

# Deploy production
vercel --prod
```

---

### 3. 🔗 เชื่อมต่อโดเมนกับ Project

**หลังจาก deploy project แล้ว:**

1. ไปที่ **Dashboard** บน Vercel
2. เลือก **Project** ที่เพิ่ง deploy
3. ไปที่ **Settings** > **Domains**
4. ใส่โดเมน: `ran-torasap.com`
5. คลิก **Add**
6. Vercel จะแสดง **DNS records** ที่ต้องตั้งค่า

**หรือ:**

1. ไปที่ **Domains** ใน Vercel Dashboard
2. คลิกที่โดเมน `ran-torasap.com`
3. คลิก **"Add to Project"**
4. เลือก project ที่ต้องการ
5. ตั้งค่า DNS records ตามที่ Vercel แสดง

---

### 4. ⚙️ ตั้งค่า DNS Records

Vercel จะบอกว่าต้องเพิ่ม DNS records อะไรบ้าง โดยทั่วไปจะต้อง:

#### สำหรับ Root Domain (ran-torasap.com):
- **Type:** A
- **Name:** @ หรือ (blank)
- **Value:** 76.76.21.21 (หรือ IP ที่ Vercel ให้มา)

#### สำหรับ www (www.ran-torasap.com):
- **Type:** CNAME
- **Name:** www
- **Value:** cname.vercel-dns.com. (หรือที่ Vercel ให้มา)

**วิธีตั้งค่า:**
1. ไปที่ Domain Settings ใน Vercel
2. คลิกที่โดเมน `ran-torasap.com`
3. คลิก "Configuration" หรือ "DNS Settings"
4. ดู DNS records ที่ Vercel แนะนำ
5. เพิ่ม records เหล่านั้นใน DNS provider ของคุณ

---

### 5. ⏳ รอ DNS Propagation

- DNS อาจใช้เวลา **5-30 นาที** หรือมากกว่า
- ตรวจสอบได้ที่: [whatsmydns.net](https://www.whatsmydns.net)
- พิมพ์ `ran-torasap.com` เพื่อดูว่า DNS propagate แล้วหรือยัง

---

### 6. ✅ ตรวจสอบ

หลังจาก DNS propagate แล้ว:
- เปิดเบราว์เซอร์ไปที่ `https://ran-torasap.com`
- ควรเห็นเว็บไซต์ของคุณ!

---

## 🔒 HTTPS (SSL Certificate)

- Vercel จะออก SSL certificate อัตโนมัติให้
- ไม่ต้องทำอะไรเพิ่มเติม
- จะใช้เวลาประมาณ 1-2 นาทีหลังจาก DNS propagate

---

## 📝 หมายเหตุสำคัญ

### ✅ ข้อมูลใน lib/data.json:
- ข้อมูลสินค้า/โปรโมชั่นจะถูก commit ขึ้น GitHub
- เมื่อแก้ไขในเว็บไซต์ production ข้อมูลจะถูกบันทึกใน Vercel
- **แนะนำ:** ในอนาคตควรเปลี่ยนเป็น database จริง (MongoDB, PostgreSQL)

### ✅ Environment Variables:
- ตั้งค่าใน Vercel Project Settings > Environment Variables
- ใช้สำหรับ SMTP email (ถ้าต้องการ)

### ✅ Custom Domain ฟรี:
- Vercel ให้บริการ custom domain ฟรี
- ไม่มีค่าใช้จ่ายเพิ่มเติม

---

## 🆘 ปัญหาที่อาจพบ

### DNS ไม่ทำงาน?
- ตรวจสอบว่าใส่ DNS records ถูกต้องหรือไม่
- รอให้ DNS propagate (อาจใช้เวลา 24-48 ชั่วโมงในบางกรณี)
- ตรวจสอบที่ [whatsmydns.net](https://www.whatsmydns.net)

### SSL Certificate ไม่ออก?
- รอสักครู่ (1-2 นาที)
- ตรวจสอบว่า DNS records ถูกต้อง
- ลอง force refresh browser

### Domain ไม่ redirect ไปยังเว็บไซต์?
- ตรวจสอบว่า project ถูก deploy แล้ว
- ตรวจสอบว่าเชื่อมต่อ domain กับ project แล้ว
- ดู logs ใน Vercel dashboard

---

## 📞 ต้องการความช่วยเหลือ?

- Vercel Documentation: [vercel.com/docs](https://vercel.com/docs)
- Vercel Support: support@vercel.com

---

## 🎉 หลังจากเสร็จสมบูรณ์

1. ✅ ยืนยันอีเมล
2. ✅ Deploy project ขึ้น Vercel
3. ✅ เชื่อมต่อโดเมนกับ project
4. ✅ ตั้งค่า DNS records
5. ✅ รอ DNS propagate
6. ✅ เปิด `https://ran-torasap.com` ใช้งานได้!

